public enum BotState
{
    Idle,
    Attack,
    Defence
}

public enum BotSubState
{
    Idle,
    Before,
    Act,
    After
}

public enum AnimatorState
{
    Idle,
    GotHit,
    Attack,
    Defence,
    Defeated,
}
